n = int(input())
import math
ans = n
p = int(math.sqrt(n))
for i in range(2,p):
    if n %i==0:
        ans = ans - ans//i
        while n % i==0:
            n = n//i
        
if n>1:
    ans = ans - ans // n
    
print(ans)
